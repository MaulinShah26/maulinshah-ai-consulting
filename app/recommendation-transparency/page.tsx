import RecommendationTransparency from "@/components/RecommendationTransparency";

export const metadata = {
  title: "Why Recommended for You Often Is Not for You | Interactive Prototype",
  description:
    "An interactive ecommerce prototype that reveals what Recommended, Best Seller, Top Rated, Popular and Sponsored labels are actually optimized for.",
};

export default function RecommendationTransparencyPage() {
  return <RecommendationTransparency />;
}
